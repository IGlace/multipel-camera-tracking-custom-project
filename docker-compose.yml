services:
  mcmt:
    build:
      context: .
      dockerfile: Dockerfile
    image: mcmt-platform:dev
    volumes:
      - ./:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
